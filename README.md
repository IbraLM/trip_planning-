Application requiert la creation d un utilisateur user et une base de donne soiree

sql:"CREATE USER 'user'@'localhost' IDENTIFIED BY 'user';"

sql:"GRANT ALL PRIVILEGES ON * . * TO 'user'@'localhost';"